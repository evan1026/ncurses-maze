file(RELATIVE_PATH CURR_DIR ${ROOT_DIR} ${CMAKE_CURRENT_SOURCE_DIR})
MESSAGE("Adding ${CURR_DIR}")

set(SOURCES "${SOURCES} ${CURR_DIR}/ASCIIMazeRenderer.cpp
                        ${CURR_DIR}/ColorMazeRenderer.cpp
                        ${CURR_DIR}/EASCIIMazeRenderer.cpp
                        ${CURR_DIR}/MazeRenderer.cpp"
                        CACHE STRING "Sources for project" FORCE)
